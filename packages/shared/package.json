{
  "name": "@mundotec/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Tipos y utilidades TypeScript compartidas entre apps del ERP.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "echo \"sin tests todavía\" && exit 0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
